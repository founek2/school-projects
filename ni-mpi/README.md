# NI-MPI

Spuštění programu:

```
python3 hw0/main.py
```

Provede iterativní výpočet pro `γ` in [5, 2, 1/2, 3, 1, 15] oběmi metodami. Pro každé `γ` vypočte výsledek, zobrazí graf pro vývoje chyby vůči iteracím, vypíše počet iterací potřebných v výpočetu a zda byla splněna podmínka konvergence.
